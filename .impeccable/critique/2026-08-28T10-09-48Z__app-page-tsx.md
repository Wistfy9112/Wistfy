---
target: app/page.tsx
total_score: 21
max_score: 32
na_heuristics: 7,10
p0_count: 1
p1_count: 2
timestamp: 2026-08-28T10-09-48Z
slug: app-page-tsx
---
Method: dual-agent (A: ses_fb82cc77affed2Pf1sAIKcox77 · B: ses_fb82b282dffeU0mrWg8QOjsfRu)

#### Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | `Navbar.tsx:16` scroll-spy + `SystemField3D.tsx:21` INITIALIZING FIELD good; gap: no progress on slow WebGL before `CoreScene` staged opacity |
| 2 | Match System / Real World | 3 | Instrument `88px grid` → `WistfyIdentity GRID_PX` coherent; weak: `ProjectViz candles` + amber line is trading idiom without legend for non-finance `Jordan` |
| 3 | User Control and Freedom | 3 | Escape + focus trap `Navbar.tsx:40` + `scroll-mt-20` anchors good; `Reveal once:true` cannot be re-triggered or disabled beyond reduced-motion |
| 4 | Consistency and Standards | 2 | Token drift: `Work.tsx:78` `border-edge` vs `171` `border-hair` for same Read case study; `lg:py-20` vs `lg:py-24` rhythm drift |
| 5 | Error Prevention | 3 | `SceneBoundary` + `webglAvailable()` fallback good; `Work.tsx:67` `role.split` silently deletes proof — prevention at cost of truth |
| 6 | Recognition Rather Than Recall | 2 | Truncated role forces recall; `Work.tsx:145` `hidden sm:inline {project.viz}` (`platform`) with no preview requires recall of codename |
| 7 | Flexibility and Efficiency | n/a | Persuade portfolio — correctly n/a |
| 8 | Aesthetic and Minimalist Design | 3 | Hero rebalance `1.08fr/0.92fr` + field capped `340→540/52vh` + lockup `max-w-[860] 0.96` succeeds; still duplicate `HUD CORE ACTIVE` in 3D + wordmark |
| 9 | Error Recovery | 2 | `SceneBoundary` `return null` gives blank field no retry message; landing has no form errors |
| 10 | Help and Documentation | n/a | Portfolio — correctly n/a |
| **Total** | | **21/32** | **Acceptable (65.6%) — just below Good (70%), solid foundation with sharp edges** |

> 21/32 = 65.6%. Two heuristics n/a (7,10) per Persuade mode — renormalized. Prior Hero alone was 17/32 (53%); Work editorial + Hero rebalance lifted +4.

#### Design Specificity Verdict

**LLM assessment: Grounded — with 2 generic leaks (7.5/10).** Hero rebalance and Work 12-col overhaul cement `DESIGN.md:Overview` Precision Field thesis. Identity is coded artifacts not skin: `Hero.tsx:113` height-capped field `340→540 max-h-[52vh]` + `CoreScene.tsx:43` orbits `0.58/0.24/0.18` + `globals.css:14` hair/edge tonal depth + `WistfyIdentity.tsx:198` W truss + `CoreScene.tsx:294` geodesic cages. That cannot be swapped without rebuilding. Two leaks: `site.ts:5` intro “design and build software systems…” is interchangeable SaaS speak (zero VO HUY voice) and `Work.tsx:145` leaks internal `project.viz` codenames (`candles`) as UI.

**Deterministic scan:** 11 advisories, 0 errors. All `advisory`.

- `Work.tsx:39,116` 108px / 96px+118px ghost indices (`text-fg/[0.035]`, `aria-hidden`) — decorative background numbers
- `Work.tsx:57,152` 15px / 15.5px summaries (`text-[15px]`) — editorial body variant vs `DESIGN.md body 17px`
- `Work.tsx:148` 2.6rem title (`lg:text-[2.6rem]`) — between title/headline ramp
- `SystemField3D.tsx:23,158` 9px instrument HUD (`text-[9px] tracking-[0.2em]`) — below `label 11px`
- `globals.css:134` `codex-grid-background` two-axis grid 88px
- `CursorRing.tsx:82` 9px cursor label (outside Work/Hero scope)
- `Hero.tsx` alone: 0 findings — Hero clean post-rebalance

**Weave:** Detector caught nothing LLM missed that matters — all advisories are intentional drift or false positives. LLM flagged ghost index hierarchy competition and HUD duplication; detector flagged the same ghost numbers as off-ramp sizes — agreement on symptom, different diagnosis. LLM correctly called intro generic and role truncation; detector cannot see copy truth. Grid at `globals.css:134` is effective false positive: `DESIGN.md:Layout` 88px datum intentional, single fixed `bg-grid` `rgba(0.024)` — not slop. 9px HUD and 108px ghost are false positives: they are calibrated instrument tiers (`viz-text 0.85`, `pointer-events-none`) intentionally off-ramp. If noisy, add to `design.json` ramp or `ignoreValues: ["9px","108px"]`.

**Visual overlays:** No browser automation available — no overlay injection, no console findings. Fallback signal: `SKIPPED (no automation exposed)`. No synthetic overlay claim.

#### Overall Impression

Calm confidence restored. Hero used to sell a screensaver at 70% weight; at `1.08/0.92` + capped `52vh` it now sells a person who *has* a system. Work finally breathes — `7/5 ↔5/7` rhythm plus full-width spread breaks masonry fatigue while 12-col datum keeps it engineered. Biggest opportunity: make Work prove external value before self-celebration. The full-bleed `03 Portfolio` spread peaks on self-reference when `01 Stock Trading` has the strongest metrics (`24/7 <1s 0 violations`) to earn that spread.

**Cognitive load:** 2/8 flagged = low-moderate (good). Passes measure (52ch hero, 48-52ch work), choices (binary hero CTAs), hierarchy (tight `mt-2.5/4` vs generous `py-12/24`). Flagged: working memory (positional `7/5→12→6/6` + re-parse per row + truncated role) and language (XGBoost/idempotent assumes literacy — correct for audience, but floor raised).

**Emotional journey:** Entry Peak (quiet pill → `VO HUY` tight `mt-2.5` → capped field discovers) → Middle Suspense (`7/5` familiar → `5/7` novelty → self-referential dip at full spread) → End Reassurance (`Stack bg-panel/40` + Lab “no images” honesty). Valley is mid-Work self-peak; add one result metric in Work list view to bridge.

#### What's Working

1. **Hero rebalance is textbook.** Owning height in `Hero.tsx:113` not `SystemField3D` decouples layout from viz weight. `340/400/440/500/540 + max-h-[52vh]` + lockup `max-w-[860] 0.96` recasts `WISTFY` from second hero to colophon — exactly `DESIGN.md` Signal Rarity.
2. **Work editorial breathes.** `Work.tsx:122` DOM visual-first is mobile-correct; `7/5 ↔5/7` + `isFull 12` + `6/6 calm` + ghost `fg/[0.035]` marks are just-perceptible craft that breaks card-masonry without losing grid.
3. **Toned system keeps rarity.** Orbits `0.58/0.24/0.18` + `isDark?0.22:0.40` + fog `8.8→15.2` hold Signal Blue ≤10% viewport while graphite `base→panel→raised` does depth — motion remains restrained and `IntersectionObserver 80px` pauses.

#### Priority Issues

**[P0] Work self-peak inversion — narrative credibility**
- **What:** `Work.tsx:26` `isFull = i===2` makes `03 Portfolio / Digital Platform` the only cinematic spread, louder than `01 Stock Trading` with real risk proof.
- **Why:** Hiring scan in order sees self-celebration before user value — peak-end memory becomes meta, not transferable.
- **Fix:** Move `isFull` to `i===0` (Stock Trading) and render `03` as `6/6` calm, or keep spread but lead copy with problem/metric not meta (“This site proves …” → “Risk-guarded execution <1s”).
- **Suggested command:** `/impeccable layout`

**[P1] Role truncation deletes proof**
- **What:** `Work.tsx:67,160` `role.split("—")[0].trim()` hides “system design to deployment” / “product & engineering”.
- **Why:** That suffix *is* the systems thesis — deleting it saves line-wrap at cost of credential.
- **Fix:** Remove split, allow 2-line wrap `leading-snug max-w-[48ch]`, or stack Role/Stack vertically on narrow `lg` 4-col text.
- **Suggested command:** `/impeccable clarify`

**[P1] DOM order vs visual order violates focus flow**
- **What:** `Work.tsx:122` `order-1/lg:order-2` + `136` `lg:order-1` keeps DOM `visual→text` while desktop shows `text→visual` on flip rows. Tab jumps across page — fails WCAG 1.3.2/2.4.3.
- **Why:** Mobile priority (visual first) bought at cost of keyboard order.
- **Fix:** Render branches conditionally (`if(flip) text→visual` in JSX) instead of `order`, or use `grid-template-areas` that preserve logical order; keep mobile visual-first via source order per breakpoint.
- **Suggested command:** `/impeccable audit`

**[P2] Split CTA token inconsistency**
- **What:** `Work.tsx:78` `border-edge` vs `171` `border-hair` for identical “Read case study” — `hair 0.07` vs `edge 0.14` is elevation language.
- **Why:** Two weights for one meaning teaches eye not to trust borders.
- **Fix:** Normalize to `border-hair group-hover:border-edge` (matches viz `group-hover:border-edge` `Work.tsx:123`) for all rows; use `bg-panel/20` for isFull demotion, not border weight.
- **Suggested command:** `/impeccable polish`

**[P2] Hinge + HUD are brittle illustration, not system**
- **What:** `Hero.tsx:118` `left:72% w-[62px]` + `SystemField3D.tsx:169` `right-full mr-2 w-[62px]` magic-positioned to perspective `CoreScene.tsx:846` `lookAt(0.42,-0.04,0)`. Drifts 16-24px from lg→xl. HUD `X:721 Y:285` `requestAnimationFrame 90ms` is `pointer-events-none` with no `aria-live` sync.
- **Why:** Illustration that pretends to be measurement breaks trust on resize/inspect.
- **Fix:** Bind hinge to measured `AXIS_X` or remove — `border-t border-hair` lockup already ties. Hide HUD churn from AT (`aria-hidden`) or sync live region.
- **Suggested command:** `/impeccable polish`

#### Persona Red Flags

**Jordan (First-timer, lands cold from LinkedIn):** First action clear? `Hero.tsx:82` View Projects vs Contact both `text-xs 0.18em` — parity forces decision without guidance. Icon-only `ArrowUpRight 13` with no label text beyond. Technical `viz` label `platform` `Work.tsx:145` assumed known. Will hesitate at hero then abandon before full spread.

**Casey (Distracted Mobile, one thumb, 4G):** Primary CTAs at `Hero.tsx:78 mt-8` near top — good. But `Work.tsx:35` `lg:py-24` full spread `aspect-[21/7]` letterboxes to thin strip on 375px — viz illegible, thumb must pinch. Heavy `CoreScene dpr [1,1.5]` still loads 980+180+95 points on first paint; no `loading="lazy"` for below-fold Work vizes beyond `whileInView`.

**Sam (Keyboard / Light-mode / Reduced-motion):** Light mode `globals.css:44` `--dim #55555c` on `#fafafa` passes, but `meta-label 11px 0.18em text-faint #6e6e78` `Work.tsx:46` fails small-text AA. Ghost index `fg/[0.035]` invisible in light (`Work.tsx:114`). `WistfyIdentity` parallax `farX/nearX/wX` still runs `useTransform` when `reduced` only short-circuits opacity — Sam gets motion without consent.

#### Minor Observations

- `Hero.tsx:134` lockup `delay 0.42` after hero `stagger 0.08` leaves ~0.3s headless gap on fast scroll — tighten to 0.30.
- `SectionHeader.tsx:20` hair rule `flex-1` overruns lede `max-w-2xl` — cap rule to grid or align to content measure.
- `ProjectViz.tsx:53` inner `FIG / 01` competes with `Work.tsx:127` absolute `FIG.01` — duplicate caption.
- `Stack.tsx:9` connector `border-edge` diamond vs `About.tsx:58` `bg-accent` vertical — two flow languages.
- `SystemField3D.tsx:158` `9px 0.2em` HUD may trigger iOS zoom — keep ≥11px or ensure `touch-action` guard.
- `Navbar.tsx:68` `z-[70]` over `ScrollProgress z-[60]` can clip progress under `bg-base/85` blur.

#### Questions to Consider

- If you removed the field for 24h, would visitors still call this “The Precision Field” — what non-3D element carries thesis alone?
- Should the editorial spread be data-driven (`project.featured`) not `i===2` index-coupled — how fragile is adding project 05?
- Two display marks (`VO HUY 6.5rem` + `WISTFY 860px`) compete in one viewport — which single mark would you keep if forced?
